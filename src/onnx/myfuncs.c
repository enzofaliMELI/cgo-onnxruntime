// myfuncs.c
#include <stdio.h>

// A simple C function that takes two integers, adds them, and prints the result.
void addNumbers(int a, int b) {
    int result = a + b;
    printf("The result of adding %d and %d is %d\n", a, b, result);
}